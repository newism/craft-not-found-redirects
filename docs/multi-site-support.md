# Multi-Site Support

The plugin fully supports multi-site installs. Site-specific UI (site selector, site columns in tables and widgets) only appears when more than one site with URLs exists, keeping things clean for single-site users.

Redirects can be scoped to a specific site or set to apply across all sites. 404s are always recorded against the site they occurred on. Craft's native site selector is used for filtering throughout.
